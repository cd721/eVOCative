<article id="users-page" class="index-container">
    <h1>Users</h1>
    <ul>
        {{#each users}}
        <li>
            <a href="/users/{{_id}}">{{firstName}} {{lastName}}</a>
        </li>
        {{/each}}
    </ul>
</article>