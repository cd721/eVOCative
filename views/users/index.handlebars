<section>
    <h1>Users</h1>
    {{#each users}}
    <article class="user">
        <h2><a href="/users/{{_id}}">{{firstName}} {{lastName}}</a></h2>
        {{{user}}}
    </article>
    {{/each}}
</section>