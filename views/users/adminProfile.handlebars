<div class="profile">
    <div id="user-info">
        <h2>I'm an admin.</h2>
        <h3>{{user.firstName}} {{user.lastName}}</h3>
        <h4>@{{user.username}}</h4>
        <p>User since {{dateProfile user.start_date}}</p>
    </div>

    <div id="admin-panel">
        <h2>Admin Panel</h2>

        <h3>Tickets</h3>
        <ul>
            <li>
                <a href="/admin/tickets">
                    <p>Check Tickets</p>
                </a>
            </li>
        </ul>

        <h3>Word Database</h3>
        <ul>
            <li>
                <a href="/admin/addNewWord">
                    <p>Add a New Word</p>
                </a>
            </li>
            <li>
                <a href="/words/all">
                    <p>See All Words</p>
                </a>
            </li>
        </ul>

        <h3>Users Database</h3>
        <ul>
            <li>
                <a href="/users">
                    <p>View Users</p>
                </a>
            </li>
            <li>
                <a href="/admin">
                    <p>View Admins</p>
                </a>
            </li>
        </ul>
    </div>

    <div id="user-stats">
        <h2>Statistics</h2>
        <ul>
            <li>
                {{#if streakOneDay}}
                <p>Streak</p>
                <h4>{{user.streak}} day</h4>
                {{else}}
                <p>Streak</p>
                <h4>{{user.streak}} days</h4>
                {{/if}}
            </li>
            <li>
                {{#if longestStreakOneDay}}
                <p>Longest Streak</p>
                <h4>{{user.longest_streak}} day</h4>
                {{else}}
                <p>Longest Streak</p>
                <h4>{{user.longest_streak}} days</h4>
                {{/if}}
            </li>
            <li>
                <p>Accuracy Score</p>
                <h4>{{toPercentage user.accuracy_score}}%</h4>
            </li>
            <li>
                <p>Total Words</p>
                <h4>{{user.words.length}} words</h4>
            </li>
        </ul>
    </div>

    <div id="user-words">
        <h2>Word Bank</h2>
        {{#if words}}
        <div class="words-container">
            <ul>
                {{#each words}}
                <li>
                    <a href="/words/{{this._id}}">
                        <h4>{{this.word}}</h4>
                        <p>{{this.definition}}</p>
                    </a>
                </li>
                {{/each}}
            </ul>
        {{else}}
        <div id="no-words" class="words-container">
            <h3>No words yet!</h3>
            <a href="/words">Add some words</a>
        </div>
        {{/if}}


    </div>
    {{#if WOD}}
    <div id="WOD-Announcement">
        <h3>You have collected the Word of the day!</h3>
    </div>
    {{/if}}
</div>